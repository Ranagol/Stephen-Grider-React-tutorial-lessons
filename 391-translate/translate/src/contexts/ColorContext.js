import React from "react";

//THIS IS THE CONTEXT OBJECT
//here, inside the () we could create a default value as a color. This is optional, not mandatory.
export default React.createContext('red');

